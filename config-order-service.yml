server:
  port: 7003

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:postgresql://localhost:5432/orderService
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
